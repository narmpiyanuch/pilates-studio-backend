const prisma = require('../utils/prisma');
const { transactionSchema } = require('../validators/transaction-validate');

exports.createTransaction = async (req, res, next) => {
    try {

    } catch (err) {
        next(err)
    }
}